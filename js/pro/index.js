// Placeholder. The Pro agent replaces this file with the real entry module that
// imports and initialises patterns.js, streaks.js, paywall.js, capture.js,
// night.js and soundscapes.js. Every app page already loads it, so shipping the
// real module needs no HTML change. Keep it a valid ES module.
export const PRO_MODULE_READY = false;
